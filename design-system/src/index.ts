// Public API of the design-system library. Re-exports the decoupled,
// dependency-free presentational components from the app's src/components.
// Scoped to a small clean-named set for the first path-B build; expand once the
// build + design-sync discovery are proven end-to-end.
export { BrandMark } from "@/components/BrandMark";
export { EmptyState } from "@/components/EmptyState";
export { PageHeader } from "@/components/PageHeader";
export { SourceBadge } from "@/components/SourceBadge";
export { PostCardView } from "@/components/PostCardView";
