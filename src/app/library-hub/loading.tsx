import { RouteLoading } from "@/components/RouteLoading";

export default function LibraryHubLoading() {
  return <RouteLoading label="Library Hub" title="Loading shared libraries" stats={3} rows={4} />;
}
